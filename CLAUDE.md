# CLAUDE.md

Context for Claude about the Mitts project.

## Workflow

Complex features (AI state machines, new systems, architectural changes) are designed first in Claude.ai chat mode, where the developer can iterate on ideas without implementation pressure. The resulting plan is then handed to Claude Code to implement against the actual codebase. When a session starts with a plan document, treat it as the agreed design — ask clarifying questions before deviating from it.

**Before every commit:** update Known Issues in this file. When a feature ships to players, update README's What's In / Planned. When a build stage completes, update ARCHITECTURE's Build Status table.

**Never push to `main` without the user testing locally first.** Feature branches (e.g. `claude/*`) may be pushed after committing so the user can pull and test on their machine. Merging a feature branch into `main` is done by the user via a pull request in the UI — do not `git merge` a feature branch into `main` directly. For work done directly on `main`, always stop at commit and wait for explicit confirmation before running `git push`.

**Scene files (`.tscn`) and complex resource files (`.tres`) are edited by the user, not Claude.** Godot's text formats are error-prone to edit when they contain node unique IDs, sub-resource references, or property ordering that the editor enforces — multi-node scenes, themes, shader materials, animations, etc. Describe the change and let the user make it in the Godot editor. **Trivial single-resource `.tres` files (e.g. `PhysicsMaterial`, simple `StandardMaterial3D`) are safe to author directly** — they're 3-5 lines with no cross-references. The UID line is optional; Godot generates one on first import if omitted.

**You cannot run the game or the test suite.** The GUT panel runs in the Godot editor; the headless CLI does not work in this environment. After touching domain code, note which test files cover the affected area and ask the user to run them. For gameplay or networking changes, describe what to test in a local session and wait for the user to verify.

**If you spot a bug or code smell while working on something else, flag it.** Don't silently fix it (out of scope), don't silently ignore it (it'll rot), don't tack it onto the current commit (muddies the diff). Surface it in chat with a one-line description and let the user decide: fix now as a small follow-up, defer to a separate task, or capture as a Known Issue here. Latent bugs in adjacent code paths are especially worth flagging — they often pair with whatever you're touching.

## What This Is

A 3v3 arcade hockey game built in Godot 4.6.2 (GDScript, 3D). Online multiplayer — one player per machine, each with their own camera and local simulation. Prioritizes feel over realism: deep stickhandling, multiple shot types, satisfying puck physics.

**Puck RigidBody3D has Continuous CD enabled.** Do not suggest enabling CCD as a fix for puck tunnelling — it is already on. Puck escaping the rink is more likely a velocity/reflection compounding bug or a Jolt edge case.

## Tech Stack

- **Engine:** Godot 4.6.2 (Jolt Physics)
- **Language:** GDScript
- **Physics tick:** 240 Hz
- **Testing:** GUT v9.6.0 under `addons/gut/`; tests in `tests/unit/` (rules/, state/, game/). Run via GUT panel in the Godot editor.
- **CI:** `.github/workflows/test.yml` runs GUT on every push and PR; `deploy.yml`'s export job gates on tests passing.
- **Deployment:** GitHub Actions → Windows + Linux export → GitHub Releases (tag: `latest`)

## Layer Architecture

The codebase is split into three layers; dependencies always flow downward:

- **Domain** (`Scripts/domain/`) — pure GDScript, no engine APIs. Rule classes (static methods), the game state machine (RefCounted), enums, and game-rule constants. Fully unit-testable without Godot.
- **Application** — `GameManager` (autoload orchestrator), controllers, `ActorSpawner`, and six RefCounted collaborators. Use the domain to make decisions; reach into infrastructure to execute them.
- **Infrastructure** — actor nodes (Skater, Puck, Goalie), `NetworkManager`, UI. The Godot-side glue.

Lower layers never reach up: actors take their collaborators via `setup()` (e.g. `Puck.set_team_resolver(Callable)`); controllers take a `game_state: Node` exposing `is_host()` / `is_movement_locked()`). Upward communication is by signals that the orchestrator listens to.

## Autoloads

Initialized in this order: `PlayerPrefs` → `Constants` → `BuildInfo` → `SoundManager` (`sound_manager.gd`, no class_name) → `NetworkManager` → `NetworkSimManager` (`network_sim.gd`, no class_name) → `GameManager`. `NetworkManager._ready()` is a no-op; the menu drives initialization. `SoundManager` exposes `play_ui(sound: SoundManager.Sound, volume_db: float)` and `play_world(sound: SoundManager.Sound, pos: Vector3, volume_db: float)`; sound constants live in its `Sound` enum.

## Confusing Boundaries

**`GameStateMachine` vs `PhaseCoordinator` vs `GameManager`:** `GameStateMachine` is a domain `RefCounted` — pure state, no signals, no engine refs, lives on both host and client. `PhaseCoordinator` owns phase-entry side effects (puck lock, goalie reset, faceoff teleport), the goal pipeline, and the goal-replay cinematic (start/stop `GoalReplayDriver`, host-only state-machine advance on natural replay end); emits signals upward including `replay_started`/`replay_stopped`. `GameManager` owns `GoalReplayDriver` Node lifecycle (`add_child`/`queue_free`) and `ReplayFileWriter` lifecycle (open/close/rollover), but delegates cinematic driving to `PhaseCoordinator`. `GameManager` wires everything together and is the only one that **dispatches RPCs to / mutates state on** `NetworkManager`. Read-only timing queries (`estimated_host_time`, `get_latest_rtt_ms`, `is_clock_ready`, `estimated_input_stamp_time`) are an explicit exception — controllers call these directly so prediction / interpolation / input stamping can run without an orchestration hop. `LocalController.send_pickup_claim` is the one *write* exception: it dispatches an RPC directly because pickup-claim eligibility is checked on every physics tick and routing the call through GameManager would add a per-tick orchestration hop for a hot-path check. Do not extend this exception to other RPCs without revisiting the rule. `NetworkManager` holds no references to controllers: it pulls outbound input batches via a `Callable` provider set by GameManager (`set_input_batch_provider`), and emits `input_batch_received(peer_id, inputs)` for inbound batches which GameManager routes through `_registry`.

**`constants.gd` vs `game_rules.gd`:** `constants.gd` (autoload) holds engine-facing values: collision layers/masks, network port, input/state rates, physics tick. `game_rules.gd` (domain) holds game-rule values: rink geometry, icing duration, faceoff positions, ice friction.

**`WorldStateCodec`** is not a pure codec — it also emits `phase_changed` / `game_over_triggered` / `period_changed` / `clock_updated` / `shots_on_goal_changed` / `queue_depth_feedback` when decoding. GameManager connects to these so it can react to authoritative host updates on clients.

**`StateBufferManager`** lives in `Scripts/game/`, not `Scripts/networking/`. Host-only pre-allocated ring buffers (720 slots = 3s at 240 Hz) for all actors. Owned by GameManager; WorldStateCodec reads `latest_*()` for broadcasts; lag-comp rewinds use `get_state_at()`.

**`GameManager` wires six collaborators:** `PlayerRegistry`, `WorldStateCodec`, `ShotOnGoalTracker`, `HitTracker`, `PhaseCoordinator`, `SlotSwapCoordinator`. Documentation that says "five" is stale.

**`SkaterController`'s five `RefCounted` collaborators live in `Scripts/controllers/`, not `domain/`.** `SkaterStateMachine` (current shot state, follow-through timer, locked aim direction), `SkaterAimingBehavior` (charge distance, sweep history, one-timer window), `SkaterPoseCoordinator` (facing, upper-body twist/lean, velocity lean, lower-body lag, head angle, angular-velocity bookkeeping), `SkaterShotPoseCoordinator` (slapper wind-up blade pose, wrister/slapper follow-through), and `SkaterIKCoordinator` (mouse → top-hand IK, bottom-hand IK, net/goalie/butterfly clamps, blade-Y geometry helpers) all carry controller-local mutable state that is tightly coupled to per-tick input processing. Domain rules are stateless static methods; these classes are stateful collaborators owned by `SkaterController` and set up in its `setup()`. The pose and shot-pose coordinators expose public state fields (`facing`, `upper_body_angle`, `lower_body_lag`, `ik_locked_side`) so `LocalController.reconcile` can snap them on replay entry/exit; `_blade_relative_angle` and `_do_release` stay on the controller because they cross multiple coordinator boundaries.

## Networking Invariants

These are non-obvious constraints that cause subtle bugs if violated. Rates and wire format are in `ARCHITECTURE.md`.

**A client's puck is always in one of three modes.** *Carried* — `_carrier_peer_id` is set, puck is pinned to the carrier's blade; no interpolation runs. *Trajectory prediction* — carrier has released; `PuckController` advances the puck forward from the release point using stored velocity and `ICE_FRICTION`, applying three-zone broadcast correction each tick (see below). *Interpolated* — no carrier, no prediction; client buffers host snapshots and renders from `estimated_host_time() - interpolation_delay`. The `carrier_idx` field decoded from world state is what `PuckController.apply_state` reads to switch non-carrier clients between prediction and interpolation modes.

**On clients, `_carrier_peer_id` is managed exclusively by reliable RPCs, never by world state.** Unreliable packet ordering conflicts with locally-predicted carrier transitions. On the server, `_carrier_peer_id` in `PuckController` is set by physics callbacks (`_on_puck_picked_up` / `_on_puck_released`). The world state does encode a `carrier_idx` field, but clients use it only to enter/exit trajectory-prediction mode — they do not write `_carrier_peer_id` from it.

**`rtt_ms` in pickup, shot, and hit claims is the raw unaveraged latest sample** (`latest_rtt_ms` from `ClockSync`), not the smoothed average. Rewind depth must track the actual current round-trip.

**Pickup claim rewind uses separate timestamps for blade and puck.** `blade_timestamp` rewinds the skater snapshot; `puck_timestamp` rewinds the puck path. They diverge when the puck was released under lag.

**Trajectory prediction exits on post contact and puck-goalie contact**, not only on carrier-change RPCs. Both controllers call `end_trajectory_prediction()` directly on the relevant physics signal.

**Goalie state transitions and shot reactions are sent via reliable RPCs** (`state_transitioned`, `shot_reaction_started` on `GoalieController`). `apply_state_transition` directly sets the client state machine; `apply_shot_reaction` seeds `_shot_timer` so the butterfly cadence matches the host. The client runs a full copy of the goalie AI every frame — do not add interpolation logic or attempt to derive goalie state from unreliable broadcasts.

**Reconcile saves and restores only narrow shot-state fields** (`_state`, follow-through timers, one-timer window, `slapper_charge_timer`). Visual and charge fields come from replay output. The `slapper_charge_timer` save/restore is required because `_update_slapper_charge` ticks the timer inside the replay loop — without it each reconcile re-ticks the unconfirmed inputs and the timer inflates O(N) per broadcast, popping the blade above `slapper_wind_up_height`. Server authority on shot state: if `server_state.shot_state` differs after replay, server wins — `_state` and `_charge_distance` are overwritten. **Two symmetric guards protect in-flight shot transitions:**
- **`FOLLOW_THROUGH` → aim state blocked.** When the client is in `FOLLOW_THROUGH` it has already sent the release RPC; the host is processing-lag behind. Reverting would loop the follow-through animation every broadcast.
- **`WRISTER_AIM` / `SLAPPER_CHARGE_WITH_PUCK` → skating blocked when `has_puck = true`.** There is a ~23ms window between the client pressing shoot and the host processing that input (60Hz batch window + network transit). World states broadcast at 40Hz have a ~90% chance of arriving during this window carrying `shot_state = SKATING_WITH_PUCK`. Without this guard reconcile resets the state machine on every broadcast; `shoot_pressed` has already fired once and `shoot_held` never re-enters aim, so the shot never releases. Gated on `has_puck` so a puck steal (which clears `has_puck` before the next reconcile) still overrides correctly.

**Mouse position is seeded from the first replayed input at replay start and the last at replay end.** Wrister-aim charge accumulation is a function of sweep distance; both endpoints must match for deterministic replay across reconcile.

**Board collision is clamped in the reconcile replay loop** via `GameRules.clamp_to_rink_inner` after each `global_position += velocity * delta` step. Without this, board-bounce divergence triggers a feedback loop of reconciles.

**Blade and top-hand positions on remote skaters are extrapolated from the body velocity field**, not from derived position deltas. Dividing position deltas by client receive-time gaps amplifies jitter into visible blade jumps.

**Post-reconcile blade pose dispatches by state on the local controller.** After replay completes, `LocalController.reconcile` re-applies the blade based on the restored `_sm.get_state()`: slapper-charge states call `_apply_slapper_blade_position`, follow-through calls the matching `_apply_*_follow_through`, shot-blocking is a no-op (block stance owns the pose), everything else calls `_apply_blade_from_mouse`. Calling `_apply_blade_from_mouse` unconditionally would IK the blade to the mouse position every reconcile, popping it down from the slapper wind-up pose at the broadcast rate (~40Hz) and producing a visible flicker against the next physics tick's slapper handler.

**Body check impulse is injected into the reconcile replay at the matching `host_timestamp`**, so replay reproduces the post-collision trajectory without oscillation.

**`LocalController.reconcile` is blocked during dead-puck phases.** `on_faceoff_positions` RPC is the authoritative source of skater positions during locked phases.

**Client inputs are applied immediately (no local delay) and stamped with `NetworkManager.estimated_input_stamp_time()`.** This stamps each input for `estimated_host_time() + INPUT_LEAD_SEC` where `INPUT_LEAD_SEC = BATCH_INTERVAL + BUFFER_TICKS * TICK_DURATION ≈ 25ms`. The lead accounts for: worst-case 60Hz batch-send delay (≈16.7ms) so the input is already in transit before the stamp expires at the host, plus a 2-tick buffer (≈8.3ms) so the host queue stays non-empty between batches. `estimated_host_time()` already encodes RTT/2 via the NTP offset — adding RTT/2 again to the stamp would double-count transit. `estimated_input_stamp_time()` is the only consumer of `INPUT_LEAD_SEC`; interpolation and all other callers use the unmodified `estimated_host_time()`.

**The host gates input consumption on `host_timestamp`.** `RemoteController._drive_from_input` only pops the front input when `host_timestamp <= NetworkManager.estimated_host_time()`. Without this gate, inputs are consumed immediately on arrival regardless of their timestamp, the queue empties between 60Hz batches, and fallback-input fires for ~4 ticks per gap. The gate falls through (`input_due = true`) when the clock is not yet ready so behaviour is unchanged during NTP warmup. When the queue is empty and not movement-locked, `NetworkTelemetry.record_input_starvation()` is called; when an input is popped, `record_input_lead(estimated_host_time() - input.host_timestamp)` records how overdue it was (target: near-zero ms, ideally ≤8.3ms).

**The pending input queue is drained on `is_movement_locked()` phases.** `is_input_blocked()` returns immediately without draining — there is no longer a queue to flush since inputs are applied the frame they are gathered.

**Trajectory prediction uses a three-zone response, not a single hard snap.** Each broadcast computes `latency_corrected.position = state.position + state.velocity * rtt_s` (full-RTT forward correction) then compares the distance to two thresholds (`trajectory_soft_blend_threshold` = 0.3 m, `trajectory_hard_snap_threshold` = 1.5 m): below 0.3 m — soft position blend (`position_correction_blend` = 0.1) plus velocity blend; 0.3–1.5 m — velocity-only blend, no position change; above 1.5 m — hard snap both position and velocity and clear the state buffer. This eliminates the visible pop that previously occurred at exactly 1.5 m.

**Body check impulse uses `body_check_impulse_applied(impulse)`, not `body_checked_player`.** `Skater` emits `body_check_impulse_applied` symmetrically: the attacker emits its rebound delta from its own `body_check_delta = velocity - vel_after_slide` capture (`skater.gd:645-647`), and the same `_resolve_player_collisions` loop also captures `other.velocity` before the transfer `-=` and emits the victim's transfer delta directly on `other` (`skater.gd:720-727`). Both sides' `LocalController` connect to the signal and inject the captured impulse into reconcile replay at the matching `host_timestamp`. Without the victim emission, only attacker rebound was captured and the victim relied entirely on snapshot authority absorbing the impulse — broken if the snapshot predated the host's resolve. The old `body_checked_player` signal only captured attacker rebound, causing consistent under-correction; that signal is still emitted but is now exclusively used for server-side puck-strip dispatch via `SkaterController._on_body_checked_player`.

**`ClockSync` does pure NTP only — do not add queue-depth feedback to it.** The clock offset (`_offset`) is computed from ping/pong samples alone: collect up to 8 samples, drop the 2 highest-RTT outliers, average the rest, EMA-smooth (α=0.3) after `is_ready`. `estimated_host_time()` adds a monotone floor so time never goes backward. Do not nudge `_offset` for any other reason (buffer depth, queue length, etc.) — any unbounded integrator on the clock drifts without limit and makes the offset display useless. On same-machine sessions the offset will be approximately equal to the time between host start and client connection (e.g. +1.3 s if the client connected 1.3 s after the host started) — this is correct after the session-relative timestamp change. The important invariant is that the offset is **stable** (not drifting over time); ongoing drift is a code bug. Buffer depth is managed separately: the client receive buffer via `_adapt_interpolation_delay()` in each controller; the host input queue depth is governed by `INPUT_LEAD_SEC` in `ClockSync` (the constants live there because they are a companion to the offset; the method `estimated_input_stamp_time()` is the only call site).

**`facing` in `SkaterNetworkState` is `Vector2` (XZ packed as XY), not `Vector3`.** Both `WorldStateCodec` and `BufferedStateInterpolator.lerp_facing` use the same compass convention: extract angle via `atan2(x, y)` (bearing from +Z/forward axis) and reconstruct with `Vector2(sin, cos)`. Do not pass a `Vector3` facing direction to `lerp_facing`. `RemoteController` and `StateBufferManager` use `BufferedStateInterpolator.hermite_angle` for C1-continuous facing and upper-body rotation interpolation, driven by `facing_angular_velocity` and `upper_body_angular_velocity` fields on `SkaterNetworkState` (encoded as s16, scale `PI * 10` rad/s, at wire offsets 27–28 and 29–30 of the 37-byte skater block).

**All interpolators use host-capture timestamps, not client arrival time.** `apply_network_state` / `apply_state` on `RemoteController`, `PuckController`, and `GoalieController` each take a `host_ts: float` parameter decoded from the world-state header. `RemoteController` and `PuckController` buffer it as the snapshot timestamp (`render_time = NetworkManager.estimated_host_time() - interpolation_delay`). `GoalieController` uses it differently — `elapsed = estimated_host_time() - host_ts` drives position forward-prediction rather than buffer lookup. Using client arrival time instead of host-capture time causes same-frame world-state packets to silently clobber each other and decouples the render timeline from the simulation timeline.

**Spectator slots use `team_id == NetworkManager.SPECTATOR_TEAM_ID` (-1) end-to-end.** Lobby keys spectators at `_SPECTATOR_KEY_BASE = 100` upward so they never collide with the 0..5 home/away keys; `LobbyManager._team_id_from_key`/`_slot_from_key` decode them. The `assign_player_slot` RPC carries `team_id = -1` for spectators (with zero-color tuples that the receiver ignores); `GameManager.on_slot_assigned` branches on this and calls `_become_local_spectator()` instead of `_registry.spawn`, mounting `SpectatorCamera` and emitting `local_spectator_state_changed(true)` for HUD chrome. Spectators are deliberately NOT in `_registry`, so every iterator that already loops `_registry.all()` (scoreboard, off-screen indicators, TOI accumulator, career stats reporter, world-state codec) excludes them naturally — do not add explicit spectator filters to those callsites. The host-only mirror for cross-peer queries is `GameManager._spectator_peers: Dictionary[int, bool]`, populated in `on_host_started` / `on_player_connected` / `_push_lobby_assignments_to_clients` and exposed via `is_spectator_peer(peer_id)` and `spectator_peer_count()`. ENet connection cap is `GameRules.MAX_CONNECTIONS = MAX_PLAYERS + MAX_SPECTATORS`; the player roster is still gated separately by `PlayerRules.MAX_PER_TEAM`.

**Mid-game spectator ↔ player swap routes through `_on_slot_swap_requested` with separate paths.** `SlotSwapCoordinator.try_swap_slot` only validates peers already in `_state_machine.players`, so promote/demote bypass it: `new_team_id == -1` runs `_demote_player_to_spectator` (drops puck if carrier, broadcasts `notify_spectator_demoted`), `_spectator_peers.has(peer_id)` runs `_promote_spectator_to_player` (validates slot/team capacity inline, spawns + RPCs the same as `_push_lobby_assignments_to_clients`). The demote RPC is a one-way "despawn this peer's skater everywhere"; promote reuses the existing `spawn_remote_skater` + `assign_player_slot` RPCs that mid-game joins use, so other peers see promotion as if a fresh peer joined. The promoted peer's `on_slot_assigned` detects mid-game by `_state_machine != null` and skips `_spawn_world()`. Demote of the local peer **must clear the input batch provider before** `_registry.remove` queue-frees the LocalController — otherwise NetworkManager's input pump would call into a freed object.

## Where New Code Goes

| Task | Location |
|------|----------|
| New game rule or geometry constant | `domain/config/game_rules.gd` |
| New pure stateless math or rule | New file in `domain/rules/` + GUT test |
| New domain state type | New file in `domain/state/` + GUT test |
| New per-player stat | `PlayerStats` → wire format → `WorldStateCodec` → `PlayerStats.to_dict()` for Supabase |
| New career stat column | Add to `career_stats` table in Supabase SQL editor → add to `career_totals` view → add to `PlayerStats.to_dict()` → add row in `CareerStatsScreen._on_totals_received` |
| Submit bug report from UI | Instantiate `BugReportDialog`, `add_child` it, call `.open()` on button press |
| New RPC | `NetworkManager` (define) → emit a signal → `GameManager._wire_network_signals()` (connect) |
| New phase-entry side effect | `PhaseCoordinator` |
| New controller behavior | Method on `SkaterController`; `GameManager` calls it, never pokes internals directly |
| New reconcile logic | `domain/rules/reconciliation_rules.gd` + GUT test |

## Code Conventions

**Strong typing everywhere.** Typed arrays (`Array[BufferedPuckState]`), typed function signatures, typed variables. Never leave a type annotation off when it can be provided. Prefer `var state: PuckNetworkState` over `var state`.

**Godot naming conventions.** `snake_case` for variables and functions, `PascalCase` for class names, `SCREAMING_SNAKE_CASE` for constants.

**Separation of concerns.** Physics bodies (`Puck`, `Skater`) expose a clean API. Controllers drive them. `GameManager` owns spawning and world state. `NetworkManager` owns RPCs. Don't reach across these boundaries casually.

**Network API uses typed objects, not raw arrays.** Functions accept `SkaterNetworkState` / `PuckNetworkState` directly. Serialization happens only at the RPC boundary.

**Get it working, then tune numbers.** Use `@export` on tunable parameters so values can be adjusted in the editor. Don't prematurely optimize or bikeshed on constants before the mechanic runs.

**Don't shy away from complexity when it improves feel.** This project already has full client-side prediction with input replay, buffered interpolation, and puck trajectory prediction with reconciliation. If adding a complex system will make the game feel meaningfully better to play, it's worth doing — think it through carefully first, then implement it properly.

**All popups and modal dialogs must be closeable via `ui_cancel` (Escape).** Add the popup to the existing `_unhandled_input` block in the relevant UI script — check `popup.visible`, hide it, and call `get_viewport().set_input_as_handled()`.

## Launch Modes

All start paths go through `MainMenu.tscn`. `NetworkManager._ready()` does nothing — the menu calls `start_offline()`, `start_host()`, or `start_client(ip)` directly. These set up ENet but defer world spawning. `Hockey.tscn`'s root node runs `game_scene.gd`, whose `_ready()` calls `NetworkManager.on_game_scene_ready()`, which emits `host_ready` on hosts; `GameManager` listens and calls `on_host_started`. Client world spawn is triggered by the `client_connected` signal from `_on_connected_to_server()`.

NetworkManager → GameManager communication is signal-based: every RPC / ENet callback emits a typed signal, and GameManager wires all connections once in `_ready()` via `_wire_network_signals()`. The only downward data flow is `NetworkManager.set_world_state_provider(Callable)`.

## Distribution

Playtester builds ship via GitHub Releases (`latest` tag). `deploy.yml` computes `VERSION=0.1.<git rev-list --count HEAD>`, rewrites the placeholder `"dev"` in `Scripts/game/build_info.gd` to that string before export, and publishes with the version as the release name. The main menu's `UpdateChecker` polls the GitHub API on startup and prompts re-download when stale. No in-game patching — Steam (SteamPipe) is the long-term plan. Don't add an in-game downloader/launcher before Steam.

**Supabase backend:** `Scripts/game/supabase_config.gd` holds the project URL and publishable (anon) key — safe to commit, RLS restricts it to INSERT/SELECT/UPDATE. `CareerStatsReporter` (`Scripts/game/career_stats_reporter.gd`) POSTs one row to `career_stats` at game-over and GETs from the `career_totals` view for the career screen. `BugReporter` (`Scripts/game/bug_reporter.gd`) POSTs to `bug_reports` with a telemetry snapshot. Both use fire-and-forget `HTTPRequest` nodes added to the scene tree root and fail silently. The secret key must never be committed — use only the publishable key in `SupabaseConfig`.

## Known Issues / Planned Work

- **AI emergency backcheck routes through the slot, not the mark:** `_is_caught_up_ice` (`Scripts/ai/skater_agent_state_machine.gd`) detects when a bot is forward of the puck on opp possession on our defensive half (NZ or DZ); when true, `_off_puck_anchor` returns `_emergency_backcheck_anchor(snapshot)` — anchored at puck depth + `BACKCHECK_BACK_OF_PUCK_M = 5 m` toward own net, centred X, capped at `BACKCHECK_MAX_DEPTH_FROM_GOAL_M = 5 m` past own goal — instead of routing through the man-to-man mark anchor. The destination tracks the puck rather than a fixed slot point so the bot doesn't over-skate when the puck is still in NZ (was anchoring at the slot regardless of puck position before — bot ended up 16 m back of a NZ-puck). Real-hockey rule: caught backcheckers skate STRAIGHT to a position back of the puck, then pick up a mark once they're there. Without this, a turnover at the OZ blue line gave up multi-second breakaways because man-to-man's anchor sometimes routed wide (mark at the boards) and the bot covered extra distance on a rotation they were already losing. The override releases automatically when the bot crosses the puck's z (no longer "caught"), and man-to-man takes over with whatever mark `coverage_assignment` gave them. Also wired into `_state_chase_puck`'s exit condition so an F1 caught up-ice drops to OFF_PUCK (and gets the backcheck anchor) instead of futilely chasing the puck heading the other way. Skipped in our OZ — a "caught" bot there is forechecking, not backchecking. Tuning: raise `BACKCHECK_BACK_OF_PUCK_M` toward 8 if bots stop too close to own net during the route; lower toward 3 if they overshoot into the goalie crease.

- **AI man-to-man defending fires on the whole defensive half (NZ + DZ), not just DZ:** `_should_play_man_to_man` (`Scripts/ai/skater_agent_state_machine.gd`) now returns true whenever the puck is on our defensive half (NZ or DZ) and we don't have own possession — previously gated to DZ only, leaving a 1-2 s transition window in the NZ where bots used zone anchors only and missed assignments on a developing rush. Skipped in our OZ (forecheck zone — F2/F3 zone anchors) and skipped under own possession (breakout, not coverage). Coverage assignment itself is unchanged — it picks marks by closest-to-own-net regardless of zone, so extending the SM gate is sufficient. With the F3 high-man pin, the deepest non-carrier teammate gets the most-dangerous mark on transitions, which is the textbook 3v3 backcheck assignment.

- **AI net-front screen on slapping teammate:** `_net_front_screen_anchor_or_zero` (`Scripts/ai/skater_agent_state_machine.gd`) detects any teammate currently in `_SHOT_STATE_SLAPPER_CHARGE_WITH_PUCK` (`SkaterStateMachine.State.SLAPPER_CHARGE_WITH_PUCK` = 3) via the broadcast `shot_state` field on `SkaterNetworkState`. The closest non-shooter teammate to the screen anchor — `SCREEN_OFFSET_FROM_GOAL_LINE_M = 2.5 m` past the attacking goal line, centred on net mouth, just outside the crease arc — anchors there for the duration of the windup. Bots don't slap themselves (only wristers), so this fires on HUMAN teammates winding up from the point. Steering's crease repel still applies but the anchor sits outside the arc + extension so the screener doesn't get pushed off. Tuning: raise `SCREEN_OFFSET_FROM_GOAL_LINE_M` toward 3.5 if the screener feels too close to the goalie; lower toward 2.0 for a tighter screen. Inserted after tag-up + give-and-go but before man-to-man / role anchors so it overrides defensive coverage during a slapper windup. Stateful behaviour — verify by running a 2-bot + 1-human team, take a slapper from the point, watch the bot park in front of the net.

- **F3 high-man pin in role assignment:** `AIRoleAssignment.compute` (`Scripts/domain/ai/role_assignment.gd`) locks the deepest non-carrier teammate to F3 outside our own DZ when they're meaningfully back of the puck (`dist_to_own_net < puck dist_to_own_net`). Without this rule, a loose puck near the high man rotated them to F1 (closest-to-puck) → CHASE_PUCK, which left the team with no last line of defense — easy 2-on-1 rushes the other way. The pin only fires when (1) puck is outside own DZ (coverage_assignment owns DZ shape), (2) team has 3+ skaters (need an F3 slot), (3) the deepest non-carrier is genuinely back of the puck (`dist_to_own_net < puck_dist`). On a full rush where everyone's forward of the puck, no pin. Pin moves the deepest entry from its puck-distance rank to index 2 (F3); whoever was originally rank 1 cascades to whichever rank is now empty. The carrier is excluded from "deepest" so a breakout from deep doesn't pin the carrier as F3.

- **AI carrier won't park behind/near goal line:** `_find_best_carry_position` (`Scripts/ai/skater_agent_state_machine.gd`) sets the current-position baseline score to `-INF` when the carrier is past the goal line buffer (`absf(self_pos.z) > absf(_attacking_goal_pos.z) - CARRY_GOAL_LINE_BUFFER_M`, typically the dead zone within ~1 m of the goal line and behind it). Forces the candidate search to pick a forward direction. Without this gate the bot would happily sit behind the net if pass score from there was decent (shoot is already 0 past the goal line via `_is_past_goal_line`). Real hockey: behind-the-net is a setup spot, not a destination.

- **AI pass scoring filters unreachable receivers via blade-ROM cone:** `_is_pass_target_reachable` (`Scripts/ai/skater_agent_state_machine.gd`) drops pass targets where the predicted receiver position sits behind the bot's current facing past `PASS_REACHABLE_DOT_MIN = 0.1` (≈84°). The bot fires a quick-shot the same tick it transitions out of CARRY → quick-shot direction is `(blade - player)` per `ShotMechanics.release_wrister`, with the blade IK clamped to the ROM (≈90° each side of facing) and the body locked from rotating past the `ik_gate` (90° + 67° = 157°) in `SkaterPoseCoordinator.apply_facing`. Without this filter the AI scored backward passes as legitimate (open receiver, clear lane), committed, and the actual quick-shot fired at the ROM edge — puck went sideways into open ice and it "looked like a dump." Symptom was concentrated in the NZ when the bot was the furthest forward (only viable receivers were behind, all unreachable). The filter uses the predicted receiver position (post-flight-time lead) since that's where the blade IK resolves toward. GUT coverage at `tests/unit/rules/test_ai_pass_reachability.gd`. Tuning targets: lower `PASS_REACHABLE_DOT_MIN` toward 0.0 (90° hard half-plane) if the filter rejects valid forward-perpendicular passes; raise toward 0.3 (~70° cone) if marginal-angle passes still misfire.

- **AI pass scoring predicts opponents AND receiver at the pass flight time, not at decision time:** `_pick_action` (`Scripts/ai/skater_agent_state_machine.gd`) now computes per-receiver `flight_t = clampf(dist / PASS_PUCK_SPEED_REF_M_S, 0.0, PASS_LEAD_MAX_S)` and projects both the receiver position and every opponent position forward by it before calling `score_pass`. Pass flight is 0.5-1.1 s typical (much longer than the 250 ms wrister charge), so evaluating `_lane_clear` and receiver pressure at decision-time positions made the AI feed defenders who were one stride from stepping into the lane. The scoring now matches what `_pass_aim_point` actually fires at press time (lead receiver) — decision and aim are consistent. New per-receiver scratch buffer `_scratch_opponents_pass` rebuilt inside the pass-scoring loop because the lookahead varies per teammate. Dump scoring still uses current positions (one-tick decision); shoot still uses the 250 ms wrister-charge predicted positions; carry-search position scoring (`_find_best_carry_position`) keeps current positions as a deliberate simplification — that's a heuristic for drift-toward-best-spot, not a commit decision, and the per-candidate × per-teammate prediction would be much more cost for marginal benefit.

- **AI shooting predicts goalie position at the wrister lookahead, not goalie current position:** `_predicted_goalie_pos` (`Scripts/ai/skater_agent_state_machine.gd`) returns `AITrajectory.predict_at(goalie_pos, goalie_vel, BOT_WRISTER_LOOKAHEAD_S)` (= 250 ms forward). Goalies actively slide on shots at 4-5 m/s; the previous code used the goalie's current position, so a sliding goalie covering the strong side reads the same as a stationary one — the bot aimed at where the goalie WAS, then 250 ms later when the wrister released the goalie was 1.1 m further over and easily made the save. Three call sites use it: `_pick_action` (shoot scoring), `_shot_aim_point` (ongoing aim during CARRY / SHOOT_PRESSED), `_find_best_carry_position` (8-direction anchor search). `GoalieNetworkState` already broadcasts `velocity_x` / `velocity_z`, so no new wire data — just the prediction step. Falls back to `_attacking_goal_pos` when goalie state isn't buffered yet (first-frame edge case); downstream geometry handles that input fine.

- **AI aim wobble uses a per-bot RNG seeded once at spawn:** `SkaterAgentStateMachine` holds `_rng: RandomNumberGenerator` seeded in `setup()` from `(peer_id * 1000003) ^ NetworkManager.host_tick`. The previous implementation allocated a fresh `RandomNumberGenerator` and called `randomize()` on every `_aim_wobble` invocation — per-shot heap allocation and replay-breaking non-determinism. Per-bot stream + session-salted seed gives each bot its own deterministic-but-distinct wobble pattern, replay-stable within a session and varied across sessions. `peer_id × 1000003` spreads bot ids (10000+) across the seed space; XOR with `host_tick` at spawn provides per-session entropy. Only call site is `_aim_wobble`; no other AI code uses random.

- **AI mid-charge wrister bail uses a forward-cone opponent check:** `_state_shoot_pressed` (`Scripts/ai/skater_agent_state_machine.gd`) calls `_opponent_within_forward(snapshot, self_pos, _attacking_goal_pos - self_pos, BOT_WRISTER_BAIL_RADIUS_M)` instead of the old omnidirectional `_opponent_within`. A defender behind or beside the shooter (no forward dot) can't realistically disrupt a wrister windup; bailing on them threw away clean shots any time a backchecker was within 2 m. Same intuition as the pressure cube falloff: only forward-cone threats count. The omnidirectional `_opponent_within` is kept around but isn't currently called — preserved as a safe fallback the new helper falls through to when `forward_dir.length()` is degenerate.

- **AI pressure scoring uses a directional cube falloff, not omnidirectional:** `AIActionScoring._opponent_density` (`Scripts/domain/ai/action_scoring.gd`) weights each in-radius opponent by `max(0, dot)^3` where `dot` is the cosine of the angle between (target → opponent) and the supplied `forward` direction. Behind = 0, perpendicular = 0, 45° forward ≈ 0.35, 30° forward ≈ 0.65, dead front = 1.0. The previous linear `0.5 + 0.5 * dot` was too lenient — it counted a 45°-forward defender as 85% of a dead-front defender and a perpendicular defender as 50%, which doesn't match the hockey intuition that defenders behind or beside the play don't pressure the carrier. **All three pressure call sites pass forward**: `score_shoot` and `score_pass` (receiver pressure) use `attacking_goal - target`, `score_dump` uses the same. The omnidirectional (Vector3.ZERO) default is kept on `_pressure` as a safety fallback but isn't currently used. `_receiver_open_score` continues to use `receiver_facing` (different intent — "is there a defender in the receiver's face?"). The shooter / pass receiver previously got "pressured" by defenders behind them or directly beside them, which had no real-hockey justification; the cube falloff with a goal-direction forward axis fixes both. Tuning targets: the cube exponent (raise to 4 for an even tighter "must be dead in front" cone, lower to 2 if 45° defenders feel too discounted).
- **Skater AI behavior pass A (3v3 tactical gaps) needs playtest tuning:** Four targeted changes to `SkaterAgentStateMachine` + `AIActionScoring` to plug the highest-impact 3v3 gaps. (1) **F3 stays high in OZ on own possession.** `_f3_anchor` now takes the snapshot and branches on `_own_team_has_possession`: when our team carries in OZ, F3 plants at the OZ blue line + `F3_OZ_OWN_POSSESSION_DEPTH_M = 2.0 m` (free-safety position). When the puck is loose or the opp has it, F3 keeps the previous trail-with-clamp behavior — chasing a loose OZ puck is a legit forecheck role and the back-of-dots cap still applies. The textbook rule: a turnover at this depth shouldn't leak a clean breakaway. (2) **Angling chase.** `_state_chase_puck` now biases the lead intercept by `CHASE_ANGLE_BIAS_M = 1.5 m` toward center-ice relative to the carrier's X (only when an opponent has the puck — loose-puck chase is unchanged). The bias is skipped when the carrier is within the bias distance of center (no inside to take away). Real defenders never chase straight at the puck — that lets the carrier cut to the middle. (3) **Give-and-go after a pass.** New constants `GIVE_AND_GO_DURATION_TICKS = 360` (1.5 s @ 240 Hz) and `GIVE_AND_GO_LEAD_M = 10.0`. `_state_pass_pressed` sets `_give_and_go_anchor` to the release pos shifted up-ice by `GIVE_AND_GO_LEAD_M`; `_off_puck_anchor` returns this anchor (capped through `_cap_offside`) for the timer duration, overriding the role anchor. CARRY entry clears the timer (return pass succeeded). The cut respects offside discipline via the existing `_cap_offside` so a NZ pass doesn't drag the bot offside while the play develops. (4) **NZ dump suppression.** `score_dump` multiplies by `NZ_DUMP_CLEAR_PATH_SUPPRESSION = 0.3` (`AIActionScoring`) when in NZ AND `_has_clear_forward_path` is true (no opponents within `NZ_DUMP_CLEAR_PATH_RADIUS_M = 3.0 m` projecting forward of the bot). Drops 0.7 → 0.21, below `ACTION_THRESHOLD = 0.25`, so DUMP loses to CARRY when the carrier has open ice. Tuning targets after first playtest: `F3_OZ_OWN_POSSESSION_DEPTH_M` (raise toward 3-4 m if F3 feels too pinned to the line during sustained OZ pressure; lower toward 1 m if too many breakaways still leak), `CHASE_ANGLE_BIAS_M` (raise toward 2.0-2.5 m for more aggressive funneling, lower if defenders consistently overshoot and let the carrier cut behind them), `GIVE_AND_GO_DURATION_TICKS` (raise to 480 = 2 s if return passes are getting cut off because the cut window expires too quickly), `GIVE_AND_GO_LEAD_M` (the right number is "just past the OZ blue line from a typical NZ release"; adjust if cuts consistently undershoot or overshoot the rush), `NZ_DUMP_CLEAR_PATH_RADIUS_M` (raise to 4-5 m for an even tighter "must dump only with a real wall" rule, lower if NZ passes still get dumped under perceived pressure). GUT coverage at `tests/unit/rules/test_ai_action_scoring.gd` (`test_dump_score_suppressed_in_nz_with_clear_forward_path`, `test_dump_score_fires_in_nz_when_forward_path_blocked`) and `tests/unit/rules/test_ai_chase_angling.gd` (5 cases). F3-high and give-and-go are stateful behaviors and rely on integration playtest verification — recipe: lobby → mark all 6 slots as bots → Start → watch for F3 staying near the OZ blue line during own possession, bots cutting up-ice after passing, no NZ dumps when there's open ice ahead, defenders funneling carriers toward the boards instead of getting beat through the middle.

- **AI one-timer setup via "demand the puck" pre-charge:** off-puck bots in good shot position pre-charge a slapper without the puck (`State.ONE_TIMER_CHARGE` in `Scripts/ai/skater_agent_state_machine.gd`), broadcasting `shot_state == SLAPPER_CHARGE_WITHOUT_PUCK = 4` via `SkaterNetworkState`. The carrier reads this from the snapshot inside `_compute_best_pass` and passes `ONE_TIMER_TARGET_BONUS = 2.0` as the `receiver_quality_bonus` arg to `AIActionScoring.score_pass` — boosting the receiver's shot-opportunity term (`max(geom, advance, open)`) without short-circuiting the multiplicative `lane` and `pressure` factors. So a slapper-charging receiver behind a blocked lane still scores 0, and one under heavy pressure still takes the pressure penalty. No telegraphed pass, no shared blackboard, no carrier delay — the receiver demands the puck just by visibly winding up.
  - **Three trigger gates** in `_should_pre_charge_slapper`: (1) own team has possession (carrier is teammate, not me); (2) `score_shoot` from my current position clears `ONE_TIMER_TRIGGER_THRESHOLD = 0.4`; (3) the carrier is on my non-blade side. Gate 3 matters because the slapper zone sits forward + blade-side of the player; for the puck to enter the zone via a pass, the puck path (carrier → me → continuation) must cross that zone. RH receiver wants the carrier on their LEFT; LH on their RIGHT. Without this gate, RH bots would charge for cross-passes coming from the wrong side and the puck would miss the zone entirely. Implemented as `_blade_side_dir(facing).dot(carrier - self) < 0`.
  - **Zone-based release** via `_compute_slapper_zone_pos(self_pos, facing)`. Zone is offset `SLAPPER_ZONE_FORWARD_M = 1.0 m` forward + `SLAPPER_ZONE_LATERAL_M = 0.8 m` toward the blade side (mirrors `SkaterController.slapper_zone_offset_x/z` so the bot's release timing matches what `try_one_timer_release` actually checks). Drop `slap_held` when puck is within `ONE_TIMER_RELEASE_DISTANCE_M = 1.0 m` of the zone center, NOT the player. Try_one_timer_release uses zone_radius (0.5 m) + velocity-scaled leniency (~0.05 s × puck speed, so ~1.6 m total at 22 m/s pass speeds), so 1.0 m at the zone sits well inside the effective fire window.
  - **Other exits**: have_puck (release immediately — fires one-timer if puck in zone, else cancels naturally via `try_one_timer_release`), gate 3 fails (carrier moved to blade side), score drops below threshold, or `ONE_TIMER_MAX_CHARGE_TICKS = 240` (1 s timeout).
  - **Carrier doesn't need teammate handedness** because the receiver self-gates on geometry — the carrier just sees `shot_state` and applies the bonus. Slapper-charging only happens when geometry is good, so the bonus naturally applies in good situations.
  - **Tuning**: lower `ONE_TIMER_TRIGGER_THRESHOLD` to 0.3 if bots rarely pre-charge; raise `ONE_TIMER_TARGET_BONUS` to 3.0 if carriers don't pick the one-timer pass; raise `ONE_TIMER_RELEASE_DISTANCE_M` to 1.3 if pucks pass through the zone without firing; lower `ONE_TIMER_MAX_CHARGE_TICKS` to 180 if bots get stuck charging when the carrier never feeds them.

- **AI takes slappers as carrier:** `SkaterAgentStateMachine` (`Scripts/ai/skater_agent_state_machine.gd`) gained `State.SLAPPER_PRESSED` and a `_state_slapper_pressed` handler symmetric to wrister: tick 0 captures aim and fires `slap_pressed`, holds `slap_held` for `BOT_SLAPPER_CHARGE_TICKS = 132` (0.55 s, ~62% into the SkaterController charge window past the 0.3 s wind-up), then drops `slap_held` to release. Aim direction is captured ONCE at tick 0 by `SkaterController._enter_slapper_charge` from `input.mouse_world_pos`, so the bot doesn't need to lerp the mouse during charge (unlike wrister which sweeps). Mid-charge bail uses `_opponent_within_forward` with `BOT_SLAPPER_BAIL_RADIUS_M = 2.5` (wider than wrister's 2.0 since the longer windup needs more cushion). `_pick_action` scores slapper alongside wrister: same `score_shoot` geometry but with opponents projected forward by `BOT_SLAPPER_LOOKAHEAD_S` (matches charge ticks), then multiplied by `SLAPPER_POWER_BONUS = 1.15` for the harder-to-stop release; whichever scores higher wins the "shoot" slot. Debug label shows `slap` vs `shoot` so you can see which the bot would pick at any given tick. Tuning: raise `SLAPPER_POWER_BONUS` toward 1.3 if bots feel under-committed when the lane is clean; lower toward 1.0 if they slap when they should wrister. Goalie's slapper-tell stance pull (`slapper_tell_depth_pull = 0.10 m`) automatically engages when bots charge slappers because `shot_state` broadcast carries `SLAPPER_CHARGE_WITH_PUCK` to all clients.

- **Skater AI bot identity uses peer_ids in the BOT_ID_BASE range, not negatives:** `AIController` (`Scripts/controllers/ai_controller.gd`) extends `SkaterController`. Bots are tracked in `PlayerRegistry._players` like humans but never correspond to an ENet connection — peer_ids are synthesized as `NetworkManager.BOT_ID_BASE (10_000) + bot_id`, ranging `[10000, 10005]`. **Do NOT use peer_id sign as a bot indicator** — the original implementation used `[-1, -6]` and quietly broke role assignment (`-1` is also a "no peer found" sentinel) and would have broken puck-carrier comparisons (`carrier_peer_id == -1` means "no carrier", which would false-match bot -1). Two helpers gate per-peer RPC dispatch: `NetworkManager.is_real_peer(peer_id)` (true for ENet connections, false for sentinels and bots) and `is_bot_peer(peer_id)`. `PlayerRecord.is_bot: bool` flags bot records when iterating the registry. Bots are accessible only via the lobby (Host → mark slots → Start); Free Play (`start_offline()`) bypasses the lobby and gets no bots — will revisit when adding offline-with-bots. `NetworkManager.host_tick: int` increments per host `_physics_process` and is reserved for future deterministic RNG salts (currently unconsumed). Full plan at `docs/specs/AI_PLAN.md`.
- **`GoalReplayDriver._goalies` array is unused:** `_goalies` is populated in `start()` from the goalie controllers but is never read — `_apply_interpolated_snapshot` and `_freeze_live_simulation` both go through `_goalie_controllers` directly. Safe to remove `_goalies` and the `gc.goalie` append loop. Also the file-level comment still says "Owned by GameManager (host only)" — `PhaseCoordinator` now drives start/stop; only Node lifecycle (add_child/queue_free) stays in GameManager.
- **Supabase free tier pauses after 7 days of inactivity:** The first Supabase request (career stats POST or bug report) after a quiet week may silently fail — `CareerStatsReporter` and `BugReporter` both fail silently with `push_warning`, so there is no player-visible crash. Wake-up takes ~30 s; in active playtesting the project never goes cold. If silent failures become a problem, a daily GitHub Action pinging `/rest/v1/career_stats?limit=1` keeps it warm for free. Upgrade to Supabase Pro ($25/mo) for guaranteed always-on uptime.
- **TOI does not accumulate when the game window is unfocused:** Godot pauses `_physics_process` on unfocused windows by default. In production (one player per machine, window always focused) this is correct. For local two-instance testing on one machine the background player will have near-zero TOI — expected, not a bug.
- **Career stats identity is UUID-only until Steam ships:** `PlayerPrefs.player_uuid` is a UUID v4 generated on first launch and persisted to `user://preferences.cfg`. It survives reinstalls on the same machine but not a fresh OS install or a different machine. When Steam integration ships, call `CareerStatsReporter.migrate_to_steam_id(uuid, steam_id)` once on first Steam session — it backfills `steam_id` on all existing rows and sets `PlayerPrefs.steam_id_linked = true` to prevent re-running. `CareerStatsScreen.fetch_totals` currently queries by UUID; update it to prefer `steam_id` post-migration.

- **Crease pushout needs playtest tuning:** Loose pucks that sit nearly stationary inside a goalie crease for `PuckController.CREASE_PUSHOUT_DELAY = 2.0 s` get a single outward velocity kick of `CREASE_PUSHOUT_SPEED = 3.0 m/s` from the host, then the timer resets — so if the kick doesn't dislodge a wedged puck, another kick fires 2 s later. Stationary check is `puck.get_puck_velocity().length() <= CREASE_STATIONARY_SPEED = 0.5 m/s`. Any blade deflect / body block / poke / pickup naturally resets the timer (speed jumps above the gate or carrier becomes non-null). Gated on `puck.pickup_locked` so dead-puck phases don't trigger it. Crease geometry is in `Scripts/domain/rules/crease_rules.gd` (NHL D-shape: arc radius 1.83 m, half-width 1.22 m). Tuning targets after first playtest: `CREASE_PUSHOUT_DELAY` (drop to 1.5 s if stuck pucks feel sluggish to free, raise to 3 s if it kicks during legitimate goalie freezes), `CREASE_PUSHOUT_SPEED` (raise if pucks aren't fully clearing the crease arc, lower if it feels like the puck teleports), `CREASE_STATIONARY_SPEED` (raise if slow-rolling pucks legitimately in play near the crease keep getting nudged, lower if a barely-moving puck wedged under the goalie isn't triggering).
- **Body-check tuning needs playtest validation:** Three changes ship together. (1) `Skater._resolve_player_collisions` now uses **relative closing velocity** along the contact normal — `(attacker_vel - victim_vel).dot(-normal)` — instead of just the attacker's solo velocity. Perpendicular victim motion no longer subtracts from impact, head-on collisions register harder, and chase-from-behind at matching speeds glances off. (2) `body_check_restitution` 0.3 → 0.25 and `body_check_transfer` 0.8 → 0.45 to stop victims launching across the zone. (3) `Puck.on_body_check` now applies a fresh per-skater pickup cooldown on the victim whenever `impact_force >= hit_pickup_cooldown_threshold` (default 6.0, same as strip threshold), default duration `hit_pickup_cooldown = 0.6 s`, taking `maxf(existing, new)` so it never shortens an existing `reattach_cooldown` from a same-tick strip. The lag-comp pickup-claim path in `game_manager.gd:_on_pickup_claim_received` was missing the `puck.is_on_cooldown(skater)` gate that the proximity path at `puck_controller.gd:171` already had — added so both pickup paths honor the new cooldown. Cooldown lives on the host's `Puck._cooldown_timers` (server-only), so reconcile is unaffected. Tuning targets after first playtest: `body_check_transfer` (lower if victims still slide too far, raise if hits feel weightless), `body_check_restitution` (raise if attacker over-commits), `hit_pickup_cooldown` 0.4–1.0 s window, `body_check_strip_threshold` (under the new closing-velocity formula head-on hits strip more easily — bump to 7 or 8 if that feels cheap).
- **HUD-on-ice redesign needs playtest tuning:** Slot color rings under skaters were replaced with a single slate-blue overlay system (`MenuStyle.HUD_SLATE` / `HUD_ICE` (alias) / `HUD_LINE_THIN` / `HUD_LINE_THICK` / `HUD_OPACITY` and the `CHARGE_*` palette in `Scripts/ui/menu_style.gd`). The previous ice-blue washed out against bright white ice under overhead arena lights; slate at higher opacity reads cleanly. Each skater now carries: a thin slate slot ring, a concentric charge ring (yellow→orange fill clockwise from 12 o'clock, red pulse at full, red flash on cancel — driven by a small `ShaderMaterial`), a thin chevron at 5 o'clock when `is_elevated` (mirrored to 7 o'clock for opposite-handed shooters so it never tucks behind the stick), a one-timer slapshot reticle composite (centre crosshair + rotating arrow + partial-arc ring whose gap stays glued to the arrow tail), and a single billboarded `Label3D` player name sitting just outside the slot ring on the screen-down axis. Charge ring + slapshot reticle are local-only (gated on `show_one_timer_indicator` in `SkaterController`); slot ring + chevron + name show on every skater. The one-timer reticle reads as one continuous outline: the partial-arc ring shrinks as the puck approaches the zone centre (full radius at the zone edge → `_SLAPPER_RING_MIN_SCALE` at the centre), its gap widens to keep the gap endpoints glued to the arrow's shaft sides at `(±_ARROW_SHAFT_HALF_W_UNIT, 0, sqrt(r² − w²))`, and the arrowhead stays fixed at `_ARROW_TIP_DISTANCE_UNIT` along the aim direction so only the shaft visibly grows. The centre crosshair is counter-scaled against the parent indicator's radius scale so it stays at fixed visual size regardless of `slapper_zone_radius`. `set_slapper_indicator_ready` / `update_slapper_indicator_window` remain no-ops. Tuning targets after first playtest: slot/charge ring radii (`Skater.RING_OUTER_R`, `CHARGE_RING_INNER_R`/`OUTER_R`), chevron proportions inside `_create_chevron_mesh` (uses `MenuStyle.HUD_LINE_THICK` = 0.045 m), one-timer composite tunables (`_SLAPPER_RING_MIN_SCALE`, `_ARROW_TIP_DISTANCE_UNIT`, `_ARROW_HEAD_LEN_UNIT`, `_ARROW_HEAD_HALF_W_UNIT`, `_ARROW_SHAFT_HALF_W_UNIT`, `_RETICLE_HALF_LENGTH`), name + chevron radii (`_NAME_RADIUS`, `_CHEVRON_RADIUS`), chevron angular offset (`_CHEVRON_OFFSET_DEG = 30.0`) and per-char `font_size` / `pixel_size`, slate color (`MenuStyle.HUD_SLATE = (0.22, 0.30, 0.42)`) and opacity (`MenuStyle.HUD_OPACITY = 0.85`), and full-charge pulse Hz (`_CHARGE_FULL_PULSE_HZ = 3.0`). Both the name `Label3D` and the chevron mesh are `top_level = true` and repositioned each tick from camera-aware screen-down axes computed in `_hud_screen_down_xz`, so they always sit below the skater regardless of `PlayerPrefs.attack_up`.
- **HUD-on-ice line thickness is 3D-fixed, not screen-constant:** `MenuStyle.HUD_LINE_THIN = 0.02` m. The thin line reads correctly at typical hockey camera distance but appears thinner at extreme zoom-outs. If playtest shows variation is distracting, upgrade to a vertex shader that expands rim verts by a per-fragment screen-pixel amount; would need to be applied uniformly across slot ring, charge ring, chevron, arrow, and reticle (~5 materials).
- **Chevron is local-only because `is_elevated` is not network-replicated:** `SkaterNetworkState` doesn't carry `is_elevated`, so remote skaters' chevrons stay hidden. To enable for opponents, add a 1-bit field to the network state (encoded into an unused bit in `shot_state` or a new byte) and have `RemoteController.apply_network_state` write `skater.is_elevated`.
- **Team colors are vote-based and resolve live in the lobby:** Each lobby player has a single "Team Color" dropdown (rendered above the slot grid) that votes for their own team's color — the team they're currently slotted on. Votes flow client→host via `NetworkManager.request_color_vote` and are fanned back out to all peers via `notify_color_vote`; everyone mirrors the vote map in `pending_color_votes`. The local pick persists across sessions in `PlayerPrefs.preferred_color_id`. Resolution runs every time `_refresh_grid` is called (vote received, slot swapped, peer joined/left) via `_recompute_resolved_colors` → `ColorVoteRules.resolve_team_colors` (`Scripts/domain/rules/color_vote_rules.gd`): per-team majority with a uniform-random tiebreak, and if both teams resolve to the same color the away team re-rolls (preferring its own remaining vote pool, falling back to the global palette minus home). Empty teams fall back to `TeamColorRegistry.DEFAULT_HOME_ID` / `DEFAULT_AWAY_ID`. **Tiebreak is sticky** — the previous winner is passed back into `resolve_team_colors` (last two args) and stays put as long as it's still in the tied set. Without this the slot-grid colors would re-roll on every unrelated vote arrival; sticky keeps the displayed colors stable and ensures what you see in the lobby is exactly what `_on_start_pressed` ships. The old `notify_team_colors` / `send_team_colors` lobby RPC flow was removed; mid-game `notify_join_in_progress` still carries the resolved IDs as before.
- **Slot colors removed:** `PlayerRules.slot_color()` is gone. Off-screen indicators (`Scripts/ui/off_screen_player_indicators.gd`) draw a jersey-colored arrow with the player's jersey number painted just inside the arrow base via `draw_string` (using `record.text_color` / `record.text_outline_color` so the digits match the on-jersey number). The scoreboard's #/POS/PLAYER columns are now uniformly white. Stick blade tape now uses the team primary color (passed as `blade_color` through `ActorSpawner.spawn_local_player`/`spawn_remote_player` to `Skater.set_player_color`) instead of the jersey color, so white-jersey teams remain visible on white ice. Team disambiguation between teammates relies on jersey numbers + names; if playtest shows that's not enough, revisit. Tuning targets in `off_screen_player_indicators.gd`: `_NUMBER_FONT_SIZE` (18 px at full scale), `_NUMBER_MIN_FONT_SIZE` (12 px floor when arrows shrink at distance), `_NUMBER_OFFSET_FACTOR` (0.95 of arrow size — places the number just inside the arrow base toward screen center).
- **Rule preset (`GameRules.RuleSet`) gates infraction detection:** `OFF` disables all infraction-driven ghosts; `ARCADE` (default) keeps the offside-ghost penalty but skips icing detection entirely; `NHL` runs both detections and currently still produces ghost penalties as a stub for the future stoppage + faceoff implementation. The preset is selected in the lobby (host-only OptionButton), synced via `NetworkManager.send_lobby_settings`, frozen onto `GameStateMachine.rule_set` at game start, and read on the host inside `GameStateMachine.check_icing_for_loose_puck` / `compute_ghost_state`. Clients also gate `LocalController._predict_offside` on `GameManager.get_rule_set()` so OFF doesn't predict a ghost the host will never apply. The reconcile fallback at `local_controller.gd:125` is naturally inert in OFF (server never sends `is_ghost = true`). When NHL stoppage logic is built, replace the ghost branch in `compute_ghost_state` / the icing consequence in `check_icing_for_loose_puck` with the new whistle + DZ-faceoff path — the plumbing is already end-to-end.
- **Off-screen player arrow constants need playtest tuning:** `OffScreenPlayerIndicators` (in `Scripts/ui/off_screen_player_indicators.gd`) draws slot-colored arrows at the viewport edge for any non-local player not currently on screen. Constants — `_ARROW_BASE_SIZE` (36 px), `_ARROW_MIN_SCALE` / `_ARROW_MAX_SCALE` (0.45 / 1.0), `_NEAR_DISTANCE` / `_FAR_DISTANCE` (6 m / 30 m for the size ramp), `_EDGE_MARGIN` (28 px) — are first-pass values and should be revisited after a session. The indicator is mounted as the first child of `HUD` so it draws beneath the scorebug; if it overlaps badly with other UI, prefer adjusting margins over reordering.
- **Tutorial needs playtest tuning:** The 11-step tutorial (`Scripts/game/tutorial_manager.gd`) is implemented but completion thresholds and staging positions haven't been tested against real player feedback. Key tuning targets: quick-shot vs wrist-shot elapsed-time threshold (`_WRIST_HOLD_MIN = 0.4s`), skating hold duration (`_SKATE_HOLD = 1.5s`), puck restage delay (`_RESTAGE_DELAY = 1.5s`), and the icing step's puck-z detection threshold (`GOAL_LINE_Z - 1.0`). The offsides step relies on `Skater.is_ghost` being set within one physics frame of staging — verify this holds in practice.
- **Sound tuning pass needed:** All game sounds are wired and playing but volumes, pitch, and timing haven't been tuned against player feedback yet. `SoundManager.play_ui/play_world` accept a `volume_db` param; `SkaterSoundController` exposes thresholds as constants. Skating sounds (skate_loop, skate_brake, skate_carve) are deferred — drop `.wav` files into `res://Sounds/` to activate. See `Sounds/CREDITS.md` for attribution requirements.
- **Eight impact/event sounds need audio files:** `puck_boards.wav`, `puck_goalie.wav`, `puck_post.wav`, `puck_goal_body.wav`, `puck_deflection.wav`, `puck_body_block.wav`, `puck_strip.wav`, `period_buzzer.wav`, `body_check.wav` are wired in `SoundManager` and `game_manager.gd` but the files don't exist yet — drop them into `res://Sounds/` to activate. Board, goal-body, deflection, body-block, and strip hits are broadcast to all clients via unreliable RPCs. `puck_post.wav` fires only on pipe contacts (posts, crossbar, mouth corner bends); skirt and net panels trigger `puck_goal_body.wav`. `period_buzzer.wav` plays at every period transition and game over. `puck_deflection.wav` covers blade redirects (`puck_touched_loose`); `puck_body_block.wav` covers body absorbs (`puck_body_blocked`); `puck_strip.wav` covers poke checks and body-check strips.
- **Goalie stick + blocker assembly is in transition:** The old `$Stick` StaticBody3D (just a flat blade collider, gated by `stick_enabled`) was scrapped. New model: blocker pad + full stick (shaft, paddle, blade) is one rigid assembly attached to the blocker hand. Single transform — `blocker_pos` + `blocker_rot` from the body config drive the whole thing. Stick angle is baked into the mesh layout (origin at the grip, shaft along local -Y, blade at the bottom with the standard lie angle); per-state `blocker_rot.x` tilts the assembly forward so the blade lands on the ice in front of the pads. Tilt values seeded as `STICK_TILT_STANDING = 36°`, `_READY = 52°`, `_BUTTERFLY = 72°`, `_RVH = 65°` (with ±25° Z roll for RVH lateral swing) — calculated from `acos(hand_y / stick_length)` assuming a 1.5m stick; will need playtest tuning. Scene side: replace `$Stick` with new child meshes under `$Blocker` (`BlockerPadMesh`, `StickShaftMesh`, `StickBladeMesh`) plus a single `StickHitbox` StaticBody3D + collider covering the full assembly. Reactive blocker saves (apply velocity-cap + reach treatment like the glove) are still pending — will land alongside the active poke check.
- **Goalie positioning + butterfly commitment overhaul needs playtest tuning:** Major changes to `GoalieController` and `goalie_behavior_rules.gd` ship together — they fix the "shuffle into perfect 5-hole" bug and the RVH/butterfly slide jank in one pass.
  1. **Threat-based tracking ("play the chest, not the puck").** `compute_threat_position` blends carrier body and puck position by `shooter_weight_standing = 0.75` (0.90 in butterfly). Rationale: the puck swings ±1.5 m during stickhandling but the carrier body barely moves, so puck-only tracking dragged the goalie laterally into 5-hole shots. With no carrier (loose puck or shot in flight) the threat collapses to pure puck. **Tracking lerp (`tracking_speed`) only applies when there's a carrier** — for loose pucks the threat snaps directly to puck position so the goalie reacts to rebounds where the puck actually is, not where it was 0.3 s ago. Tuning targets: `shooter_weight_standing` (lower = more reactive to puck, higher = more committed to body), `shooter_weight_butterfly` (raise toward 1.0 if dekes still drag the goalie out of position once down).
  2. **Arc-based positioning instead of straight-line angle bisector.** `target_arc_position` puts the goalie on a constant-radius circle around the goal center — radius is the existing depth chart against `threat_distance_to_goal` (Euclidean to goal center, not perpendicular distance to goal line). On wide threats the arc consumes radius for lateral motion, naturally pulling the goalie back in perpendicular depth — the "challenge angle" arc real goalies skate. STANDING/RECOVERING use 2D `_move_along_arc` so x and z move at matching rate (no asymmetric snap on Z when the threat angle shifts).
  3. **Butterfly is committed.** State machine adds `RECOVERING` (~0.35 s standing-back-up window where the goalie can't react/drop/RVH — the vulnerable window real coaches drill against on wraparounds and quick cross-creasers). Once dropped, the goalie cannot transition to RVH directly; must recover first. Body rotation locks once down — real goalies stay square in butterfly, head-tracking only (which we don't model). Sub-state IDLE↔SLIDING is derived from `_slide_velocity_x` magnitude (no separate enum value, no wire change — broadcast `velocity_x/z` carries it). State enum order is `STANDING, BUTTERFLY, RECOVERING, RVH_LEFT, RVH_RIGHT, READY` — `READY` appended at the end so existing replay files (which pre-date READY) keep decoding correctly.

  9. **READY half-down stance.** Distinct intermediate state between STANDING and BUTTERFLY, triggered by `_is_ready_situation()`: puck is in goalie's defensive half (perpendicular distance ≤ `ready_zone_distance = 25 m`) AND not carried by own team. STANDING when play is in opposing half or own offense holds the puck. Visual silhouette: knees bent, weight forward (`body_rot.x = 8°`), gloves dropped slightly and forward, pads still upright. Closer to butterfly than standing so the drop is a shorter pose travel. Same-team check needs `Skater.team_id` (set on spawn by `PlayerRegistry`). After butterfly RECOVERING, the goalie returns to READY (not full STANDING) when threat conditions still hold — they're not bouncing all the way upright between drops. RVH_* exits also resolve to READY/STANDING via the same situation check. Slapper-tell, shot detection, and pad pose all treat READY identically to STANDING via the `_is_upright()` predicate. **RECOVERING shares the READY body config** so the rise from butterfly is a single smooth motion: butterfly → ready, then either stay (threat persists) or continue lerping up to STANDING (threat eased) — no up-then-back-down overshoot.

  10. **Pads angled to deflect rebounds toward corners.** Goalies actively rotate their pads so the toe points outward; pucks hitting an angled pad face deflect off-axis instead of bouncing back into the slot. Approximated with fixed Y-rotation: STANDING/READY pads rotate `±8°`, BUTTERFLY pads rotate `±12°` (more aggressive since the down pads see the most rebound traffic). Tunable: `PAD_TOE_OUT_DEG_STANDING / _BUTTERFLY` constants in `_get_config`. Doesn't model active angling for specific shot trajectories — that requires a stick + read-the-shooter pass.
  4. **Butterfly slide is commit-and-ride.** Plant outside leg, push off, slide on inside pad in a STRAIGHT LINE. Destination is committed at slide-start (`compute_slide_destination` = arc point at `butterfly_radius`); friction (`slide_friction = 6.0 m/s²`) decays initial speed (`slide_initial_speed = 4.5 m/s`) until below `slide_min_speed = 0.3 m/s`. **No mid-slide corrections** — fast cross-passes can beat the slide because the goalie picked the wrong destination, exactly like the NHL. Trigger gate: `should_commit_slide` checks `|target.x − current.x| ≥ slide_trigger_distance = 0.40 m` after `slide_cooldown = 0.20 s`. Suppressed for `post_event_slide_lockout = 0.25 s` after EITHER a shot release OR puck contact with the goalie (one runtime timer, max wins). Real goalies track up until release then commit to their read — they need a beat to process before reacting to new lateral threats; deflection trajectories post-contact are likewise unpredictable. No artificial per-cycle cap — chained slides on quick cross-cross plays are legitimate; trigger threshold + cooldown + lockout gate sensibly. Slides are host-authoritative (clients don't trigger locally; the broadcast position+velocity drives client motion via the existing `apply_state` corrections — no new RPC).

     **`reaction_delay = 0.13 s` gates ALL physical responses** — the butterfly drop on low shots AND the arm reach on elevated shots. Goalies can't act on the prediction instantly; they need a beat to read the puck's vector after release before the hand starts moving or the body drops. Previously elevated reaches kicked off at frame 0 (hand snapping immediately) while low shots had the standard processing delay — asymmetric and unrealistic. `_apply_elevated_shot_reaction` early-returns while `_shot_timer > 0`, and the timer is set on every shot in `_on_puck_released` (host) and `apply_shot_reaction` (client RPC). `_update_shot_timer` only enters butterfly when the puck is currently classified low; elevated shots use the timer purely to gate the arm.

     **Glove tracks shot impact laterally** during the reaction. `_apply_elevated_shot_reaction` projects the puck to the goalie's CURRENT z plane (using `_puck_velocity_est`, position-derived so it works on both peers) and moves the glove toward that intercept x — not the goal-line impact `_shot_impact_x`, which is wide of where the puck actually passes through the goalie's plane (the goalie sits 0.4-1.2 m forward of the goal line). Glove x is clamped to `[glove_max_x_outward, glove_max_x_inward]`, with forward Z reach scaling with extension distance (`glove_max_z_reach`). The glove yaws toward the direction it's reaching (`glove_max_yaw_deg = 60°` cap, `atan2(-move_dx, -move_dz)` where `move = target - rest` in body-local frame) — reads as a deliberate snag motion, wrist following the reach trajectory rather than visually tracking the puck. Sign-flipped on `move_dx` because Godot's +Y rotation is counter-clockwise looking down +Y, so a leftward reach (move_dx < 0) needs positive yaw to point the palm at -X. Lateral and yaw targets are GOALIE-relative (`impact_x - _current_x`), not goal-relative. **Glove movement is hard-capped by `glove_react_max_speed = 2.0 m/s`** during elevated shot reactions (the rest of the body still uses `reaction_lerp_speed = 18`). Lerp-based tracking made the math vague (asymptotic); a velocity cap is exact — per-frame step = `speed * delta` via `move_toward` in `apply_body_config`. Real glove speeds are 2-3 m/s for full extension. With ~250 ms flight on a close wrister, the glove travels at most 0.5 m — enough for body/mid-net but not the 0.6-0.7 m needed for top-corner pulls; those score. Threaded as `apply_body_config(config, t, glove_max_step)`, with -1 disabling the cap. Blocker is still a static raise — will be refined in the stick / poke-check pass.

     **`detect_shot` predicts impact_y with gravity** (`cfg.gravity = 9.8`). Linear extrapolation `y + vy * t` overestimated landing height for long-range elevated shots — they arc down to ice level before reaching the net but were misclassified as elevated, so the goalie never dropped butterfly and the puck snuck through. Ballistic prediction `y + vy * t - 0.5 * g * t²` (floor-clamped at 0) handles this correctly. Re-projection in `_update_tracking` uses the same formula every frame, so an elevated shot that's tipping low live transitions to `is_low` and arms the butterfly drop timer mid-flight.

     **Lateral movement and facing rotation BOTH freeze while `_reacting_to_shot`** (any state). The goalie commits to their pre-release positioning and only reacts with body parts — butterfly drop on low shots, glove/blocker raise on elevated. No shuffling, no slides, no arc tracking, no body rotation. The freeze releases ONLY on a discrete resolving event:
     - puck contacts this goalie (`puck_touched_goalie`)
     - puck hits boards (`puck_hit_boards`)
     - puck hits a post (`puck_touched_post`)
     - puck hits the net (`puck_hit_goal_body`)
     - puck is picked up by any skater (`get_carrier() != null`)
     - `max_reaction_duration = 1.5 s` safety net fires (catches signal-dropped edge cases)

     After the event, `_reaction_clear_timer = reaction_clear_delay = 0.25 s` ticks before the freeze actually clears — gives the goalie a beat to process the resolution before deciding the next move. First event wins; subsequent events during the clear window don't shorten it. Critically, `detect_shot` re-projection failing mid-flight does NOT clear the freeze (it used to; that released the lock on shots arcing over the net or drifting wide before any actual resolution). Once the freeze clears: if state is BUTTERFLY, butterfly motion logic resumes (slides possible after lockout); if STANDING/READY, normal arc tracking resumes. `net_margin` (3.0 m, total catch zone ±3.9 m) controls shot-vs-pass classification at release: must impact within `net_half_width + net_margin` of goal center. Generous on purpose — real goalies track anything heading at their general area, even shots clearly going wide (could deflect / tip / rebound). Cross-crease passes self-filter through `detect_shot`'s velocity-direction check: low `puck_velocity.z` → huge `t_to_goal` → `impact_x` lands way off-net, so a big margin doesn't pull passes in. Resolving-event clears (pickup / boards / post / net) handle false positives.

     **RVH transition gated on `not _reacting_to_shot`.** RVH is for puck POSSESSED at sharp angles or behind the net (post-hug), not puck IN FLIGHT from one. Without this gate, a sharp-angle shot triggered reaction → next physics tick the puck was still in the defensive zone → state flipped to RVH and cleared the reaction before the goalie could act. Goalie now stays in STANDING/READY through any shot reaction; once resolved (puck contact / boards / post / net / pickup), the next tick can transition to RVH normally.
  5. **Pressure-triggered butterfly drop is gone.** The old `is_under_pressure` (any skater within 2.5 m approaching at 1+ m/s) was exploitable: charge to bait the drop, fade and pass cross-crease for ~0.7 s of free vulnerability. Butterfly entry is now shot-detection-only (existing `_on_puck_released` path). Close-quarters jam plays will be answered by the future stick + poke-check work; for now the goalie stays standing on proximity.
  6. **5-hole openness scales correctly.** During the butterfly drop animation (`_butterfly_drop_progress < 1.0`) openness snaps closed (no shuffle-while-dropping gap). IDLE butterfly: pads-touching, `_five_hole_openness → 0`. SLIDING butterfly: openness scales with slide velocity ratio up to `five_hole_butterfly_move_max = 0.18` (the trail-leg gap real cross-pass goals exploit). STANDING shuffle/T-push openness unchanged.
  7. **Slapper-charge stance tell.** Slapshots have a visible windup (`SkaterStateMachine.State.SLAPPER_CHARGE_WITH_PUCK` on the carrier — read via the new `Skater.current_shot_state` mirror, written by Local/RemoteController each tick). Goalie pulls deeper by `slapper_tell_depth_pull = 0.10 m` and raises hands +0.06 m. Pose-only — does not commit butterfly, so a fake-into-pass doesn't punish the goalie. Wrist shots have no comparable tell (release-and-react only via the existing 0.13 s `reaction_delay`).
  8. **Drop animation speed is explicit.** `butterfly_drop_speed = 0.08 s` controls how fast pads close to the floor. `_update_body_parts` sets the body-config lerp speed to `3.0 / butterfly_drop_speed` so the pads converge ≈95% by the design window — lerp is asymptotic (`1 - exp(-speed * t)`), so a 1/x factor only reaches 63% in time x. The previous 1/x left pads lerping for another ~120 ms after `_butterfly_drop_progress` hit 1.0, and a 24 m/s slot wrister arrived 5-hole-open before the pads sealed.

  **Tuning targets after first playtest:**
  - `shooter_weight_standing/butterfly` (0.75 / 0.90) — body vs puck blend.
  - `slide_initial_speed` (4.5 m/s), `slide_friction` (6.0 m/s²), `slide_trigger_distance` (0.40 m), `slide_cooldown` (0.20 s) — slide feel; raise trigger distance if slides feel twitchy, lower friction if slides die before reaching the post.
  - `post_event_slide_lockout` (0.25 s) — applies after both shot release AND puck contact. Unified with `reaction_clear_delay` so the slide gate and the freeze release on the same beat. Raise if rebounds still trigger spurious slides or if the goalie reacts to mid-shot lateral changes, lower if it misses legitimate slide opportunities right after a save.
  - `butterfly_min_hold_time` (0.35 s), `recovery_duration` (0.35 s) — the committed window. Raise hold if recovery feels too quick; raise recovery if wraparounds feel too easy.
  - `butterfly_radius` (0.40 m) — depth while down. Raise toward 0.6 if goalie reads too deep on slides.
  - `butterfly_drop_speed` (0.08 s) — pads-to-floor; raise toward 0.15 s if drop feels snappy/unrealistic, lower if 5-hole still leaks during the drop.
  - `slapper_tell_depth_pull` (0.10 m) — depth pull on read.
  - `recovery_proximity_threshold` (1.8 m, ~ crease radius) — only holds butterfly when the puck is BOTH slow AND not moving away AND inside this Euclidean distance (genuine jam at the feet). Pucks moving away always release; fast-and-approaching always holds. Lower if the goalie still hangs too long after a save; raise if it stands up while a forechecker is right on top of it.
  - `ready_zone_distance` (25 m) — perpendicular distance threshold for READY entry. Lower if the goalie crouches into READY too eagerly when the puck is at the redline; raise if the goalie stays upright while play is entering the OZ.
  - `PAD_TOE_OUT_DEG_STANDING / _BUTTERFLY` (8° / 12°, in `_get_config`) — pad rebound angling. Raise if rebounds still bounce back into the slot; lower if the visual angle reads as exaggerated or pads don't seal the post in butterfly slides.
  - Same-team carrier check is **not** wired yet — if a teammate carries with the puck close to the goalie's net, the goalie still tracks their chest (low priority since the depth chart pulls the goalie back to defensive depth when the puck is in own offensive zone).
  - Audio hooks for butterfly drop / slide push / recovery up are not wired yet; add them to `SoundManager.Sound` enum when audio drops in.
- **Top-hand ROM values are first-principles baselines:** `rom_forehand_angle_max_deg` (90°), `rom_backhand_angle_max_deg` (120°), `rom_forehand_reach_max` (0.45 m), `rom_backhand_reach_max` (0.70 m), `stick_length` (1.30 m, shaft-only butt-to-heel), `blade_length` (0.30 m), `shoulder_offset` (0.22 m) are anatomical defaults and will need playtest tuning. `upper_body_twist_ratio = 0.8` rotates the torso with the blade angle, effectively shrinking the angular demand on the top hand.
- **Stick / blade geometry split:** The IK rigid rod is the *shaft* only (hand → blade heel). The blade mesh is offset forward by `blade_length * 0.5` so the mesh center is mid-blade. `Skater.get_blade_contact_global()` returns mid-blade world position. If the mesh length changes in `Scenes/Skater.tscn`, keep `blade_length` in sync so the contact point and pickup zone stay at mid-blade.
- **IK tuning knobs:** `hand_y_max` (0.30 m) caps hand rise in the CLOSE regime. Arm bone lengths (`upper_arm_length = 0.44`, `forearm_length = 0.46`) must sum to more than `sqrt(drop² + rom_backhand_reach_max²)` (≈ 0.87 m at current drop) to avoid visible arm stretch at full backhand extension. `bottom_hand_grip_fraction` (0.25) and `bh_release_angle_deg` (67°) / `bh_release_angle_band_deg` (15°) control where the bottom hand grips and when it releases to a one-handed rest pose.
- **Wrister host-side controls need a focused pass:** The charge direction, power ramp, and blade IK feel during WRISTER_AIM haven't been tuned against player feedback yet. Needs a dedicated playtest session before iterating.
- **Elevation cap + away-from-net classification need playtest tuning:** `ShotMechanics._elevation_y` no longer clamps the raw y-direction ratio (was 0.45). Instead it caps the resulting initial vertical velocity so apex above blade ≤ `max_apex_above_blade` (default 1.5 m → `v_y_max = sqrt(2g·1.5) ≈ 5.42 m/s`). On-net shots arrive at goal line at ≤ `target_height` (cap kicks in at close range or very far range; in-cap region they hit target exactly). Additionally, when `attacking_goal_z` is non-zero (set on `LocalController` from `_team_id`), shots whose XZ direction sits outside a `toward_net_dot_threshold = 0.5` (cos 60°) cone of the goal vector use a low fixed `away_from_net_elevation = 0.10` Y instead of the ballistic math, so backward / lateral passes don't try to arc to an irrelevant goal. Tunables on `SkaterController`: `max_apex_above_blade` (raise if elevated shots feel too floored, lower if missed shots still go over glass), `toward_net_dot_threshold` (0.5 = 60° cone; raise toward 0.7 for stricter "must aim at net", lower for more permissive), `away_from_net_elevation` (small lift for clears/passes). Legacy path (no team info) still picks goal by `shot_dir.z` sign.
- **Reconcile replay skips skater-vs-skater collisions:** `LocalController.reconcile` replays inputs through movement code but does not re-run `move_and_slide` for player-player collisions. A body-check that occurs during the replay window is injected as a velocity impulse at the matching `host_timestamp` (via `body_check_impulse_applied`), which approximates the post-collision trajectory. Full re-simulation would require running all skater bodies through Jolt together in the replay loop — expensive and likely to introduce its own divergence. The impulse injection approach is the correct trade-off; do not attempt to add full collision replay without a careful design pass.
- **Input batch size is partially adaptive:** The 1-second loss-rate measurement window is fairly noisy on marginal connections (~40 samples at 40 Hz); a sliding window rather than a resetting one would improve accuracy.
- **`INPUT_LEAD_SEC` may need to become adaptive per-client:** The current fixed ~25ms lead (worst-case batch jitter + 2-tick buffer) works well for typical internet connections but may be too tight for high-jitter links or too loose (adding unnecessary input lag) on LAN. The standard AAA solution (used by Rocket League and similar) is: host measures actual input arrival jitter per peer from the incoming queue — specifically the variance of `estimated_host_time() - input.host_timestamp` at pop time — computes a target lead per peer (e.g. p95 jitter + 2-tick buffer), and sends it to the client in the world-state packet (a single float, negligible wire cost). The client lerps its local `INPUT_LEAD_SEC` toward the received target each broadcast. This keeps the buffer as small as possible for each individual connection. Prerequisite: add a per-peer jitter tracker on the host (ring buffer of recent lead samples, publish p95), extend the world-state header with a `target_input_lead_ms` field per skater slot, and have `LocalController` consume it via `ClockSync.set_input_lead`.
- **Career screen is missing the "Local Replays" tab.** Plan / spec called for three tabs: Career Totals · Recent Games · Local Replays. The third was deferred when shipping Feature C — it's an inventory of `.mreplay` files in `user://replays/` whose `game_id` is **not** present in the `recent_games_for` RPC results. Catches two scenarios that don't write a `career_stats` row: spectator-only sessions (spectators don't get rows) and replays acquired off-machine (e.g. a friend dropped you their `.mreplay`). Implementation: in `CareerStatsScreen`, after `fetch_recent_games` returns, build a `Dictionary[String, bool]` of cloud `game_id`s, then `ReplayFileIndex.list()` + `ReplayFileReader.read_header_only()` per file → filter out matches → render the rest as rows with a Watch button (same handoff as Recent Games and the main-menu browser). Empty-state copy if nothing orphaned. Small-ish (~80 lines on top of the existing tab code).
- **`BufferedStateInterpolator.BracketResult` uses `Variant` for state objects:** `from_state` and `to_state` are typed `Variant` because the same interpolator services `SkaterNetworkState`, `PuckNetworkState`, and `GoalieNetworkState`. Duck-typing works (all three have compatible field access on the fields the interpolator needs) but loses type safety at the boundary. Acceptable for now since the interpolator is internal infrastructure with three known callers; if a fourth state type appears or the field set diverges, switch to a typed base class or three concrete subclasses.
- **`WorldStateCodec` is a leaky abstraction:** It emits `phase_changed`, `game_over_triggered`, `clock_updated`, and other game-state signals from inside `decode_world_state`. A codec should decode into typed objects; a separate dispatcher should emit signals. The current design makes it hard to follow where `phase_changed` originates.
- **Reconcile replay doesn't skip non-movement inputs:** Every unconfirmed input replays through the full `_process_input` pipeline — state machine dispatch, IK solve, aiming update. At 100 ms RTT (~24 inputs) this is fine. At 500 ms on bad WiFi (~120 inputs) it becomes noticeable. Inputs that can't affect position/velocity (dead-phase frames, menu-only inputs) could be detected and short-circuited in the replay loop.
- **No integration test for the game loop:** The domain layer is well-tested but nothing covers faceoff → play → goal → reset end-to-end. This is the code path that silently breaks when `PhaseCoordinator` or `GameStateMachine` is refactored.
- **Uniform rendering should move to full UV-mapped texture painting:** The jersey body already uses a pixel-drawn `ImageTexture` (via `Image` fill + glyph stamp in `JerseyTextureGenerator`). Numbers and stripe overlays are still rendered as `QuadMesh` nodes with `material_override`, which causes z-fighting. The right approach is UV-unwrapped meshes (jersey, pants, gloves, socks) with a single composited `ImageTexture` per mesh — all stripe bands and number glyphs painted into one atlas at spawn time. Prerequisites: UV-unwrap the skater meshes in Blender so each face (back panel, front panel, sleeves, etc.) has a predictable UV island; define matching UV region constants in `JerseyTextureGenerator`; replace the overlay quad creation with a single `paint_and_apply_textures` call. Eliminates all overlay quad nodes and z-fighting.
- **Ice shader needs playtest tuning:** The ice surface (`Shaders/ice.gdshader`, applied in `HockeyRink._add_ice`) replaces the previous `StandardMaterial3D` with a `ShaderMaterial` that adds two effects on top of standard PBR. (1) **Sub-surface line tint** — lines are painted into the albedo texture as before, but the shader fades them toward `ice_fog_color` (default = `ice_color`) using a Beer-Lambert curve `1 - exp(-subsurface_fade / NdotV)` so absorption scales with the path length the light takes through the ice; default `subsurface_fade = 0.2` gives ~18% fade head-on, ~33% at 45°, ~55% near grazing. UV is also shifted by a small parallax offset based on `subsurface_depth` (default 0.012 m) so the lines feel embedded under the ice rather than printed on top. The previous `pow(grazing, falloff) * fade` form was way too gentle (only ~9% blend at typical hockey-camera angles even at fade=1.0); do not revert. (2) **Angle-varying gloss** — `roughness_head_on` (default 0.20) lerps to `roughness_grazing` (default 0.04) along `pow(1-NdotV, 2)` so the ice goes slightly diffuse looking down and mirror-sharp at grazing; `specular_strength` (default 0.6) drives F0 and the engine's Schlick/GGX path handles Fresnel reflection magnitude automatically. All exports live under `@export_group("Ice Shader")` on `HockeyRink`. Tuning targets: `subsurface_fade` (range 0–3; raise if lines still feel painted-on, lower if they disappear too quickly), `subsurface_depth` (1–3 cm reads as ice over concrete; >3 cm starts to swim), `ice_fog_color` (try a slightly cooler/lighter tint than `ice_color` for more separation), `roughness_grazing` (lower = sharper arena-light reflections at the far end), `specular_strength` (raise toward 0.8 if reflections look too washed out). Scuff normal maps were considered but deferred — once a tiling skate-scratch normal exists in `res://Textures/`, it can be sampled in the shader and applied to `NORMAL_MAP` for anisotropic specular breakup.
- **`MenuStyle` migrated to `Theme` resource — needs editor wiring + visual verification:** Default styling for `Button`, `OptionButton`, `LineEdit`, `HSlider`, `PanelContainer` lives in `Resources/MenuTheme.tres`, generated from the `MenuStyle` palette by running `tools/build_menu_theme.gd` in the editor (File → Run). To activate, regenerate the resource and set Project Settings → General → GUI → Theme → Custom = `res://Resources/MenuTheme.tres`. Toggles use the `&"ToggleButton"` theme variation and tabs flip between `&"TabButton"` and `&"TabButtonActive"` — both wired through small `MenuStyle.apply_toggle` / `apply_tab_button` helpers that now only set `theme_type_variation` plus behavior (toggle_mode, ON/OFF text). What stays in `MenuStyle`: color constants for ad-hoc Label tinting, `panel(corner, margin)` for non-default panel sizing, `close_button()` for the custom × widget. Visual verification needed across main menu, lobby, options tabs (Game/Video/Audio/Input), HUD pause menu, tutorial HUD, and loading screen — procedural styleboxes can drift slightly from a serialized version. If a control looks wrong, edit the EditorScript and re-run rather than hand-editing the .tres.

## Planned Features

**Tier 3 — larger scope:**
- **Reconnect / slot reservation:** When a peer drops, host marks the slot "reserved" for ~60 s. If the same player (matched by name) reconnects within the window, they reclaim their slot, stats, and team without restarting the game. Requires a pending-reconnect state in `GameManager` and a rejoin handshake in `NetworkManager`.
